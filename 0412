<!---<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Boolean Data Type</title>
  </head>
  <body>
    <script>
    //boolean : true or false
    // 4>1 // true
    // 1<0 // false
    // 4==4 // true
    // 4>=4 // true
    // 4<=4 // true
    // "one"=='one' // true
    // 1=='1' // *true*
    // 1==='1' // === : compare by data type
    // 1!=2 // true Not equal 
    
    </script>
  </body>
</html> --->


<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>if</title>
  </head>
  <body>
    <script>
      // if(6>4){ //6>4 -> true, false -> alert will not pop-up
      //   alert("True!");
      //   alert("True!!");
      // }

      // var a = 3;
      // if(a>2){
      //   alert("True");
      // }

      // var a = 3;
      // if(a>3){
      //     alert("Bigger than 3.")
      // }
      // else{
      //     alert("Less than or equal as 3.")
      // }

      // var a = 5;
      // if(a>3){
      //   alert(1);
      // }else if(a>4){ //그렇지 않다면 만약에
      //   alert(2);
      // }else if(a>5){
      //   alert(4);
      // }else{
      //   alert(3);
      // }

      // LOG IN PROCES
      // var userId = prompt("ID :");
      // var userPw = prompt("Password :");
      // if(userId=='jinha0719'){
      //     if(userPw==1234){
      //         alert("Your ID and Password are correct, will log-in soon.");
      //     }else{
      //       alert("Your ID is correct, but the password is incorret");
      //     }
      //   //location.href="http://www.naver.com";
      // }else{
      //   alert("Your ID is incorrect, please enter again.");
      // }

      //lOGICAL OPERATOR
      // true && true // and 그리고 , all true -> true
      // true && true && true && true // the number of logical operator is limitless
      // true && true && false // false (left to right)
      // true || false || false || false // or 또는 , even if there is only one true, the result will be 'true'
      // From the beggining, if the first operator is 'true' => 'true' , 'false' => 'false'.

      // if(userId =="jinha0719" && userPw == "1234"){
      //   alert("Welcome");
      // }
      // else{
      //   alert("Please enter again");
      // }

      // if(!0){ //if(0) consider as false, if(!0) == ture
      //   alert("True");
      // }

      // if(234){
      //   alert("True") // except 0, all the nmbers are considered as 'true'
      // }

      // if(''){ //빈문자, false , !'' => true , ' ' => true (if there is space => this is also data)
      //   alert("True");
      // }

      // if(!undefined){ //undefined == false, !undefined == true
      //   alert("True")
      // }

    </script>
  </body>
</html>



<!---SCORE--->
<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>성적표</title>
    <script>
    //Number("90") // 90
    var name = prompt("이름을 입력하세요");
    var korean = Number(prompt("국어 점수를 입력하세요"));
    var english = Number(prompt("영어 점수를 입력하세요"));
    var mathematics = Number(prompt("수학 점수를 입력하세요"));

    var average = (korean+english+mathematics)/3
    var grade;

    if(average>=90){
      grade = "A등급";
    }
    else if(average>=80){
      grade = "B등급";
    }
    else if(average>=70){
     grade = "C등급";
    }
    else{
     grade = "F등급";
    }

    document.write("<h1>"+name+"님</h1>");
    document.write("<table border='1'>");
      document.write("<tr>");
        document.write("<th>국어점수</th>");
        document.write("<th>영어점수</th>");
        document.write("<th>수학점수</th>");
        document.write("<th>평균</th>");
        document.write("<th>학점</th>");
      document.write("</tr>");
      document.write("<tr>");
        document.write("<td>"+ korean +"</td>");
        document.write("<td>"+ english +"</td>");
        document.write("<td>"+ mathematics +"</td>");
        document.write("<td>"+ average +"</td>");
        document.write("<td>"+ grade +"</td>");
      document.write("</tr>");
    document.write("</table>");

    // 90 A 80 B 70 C F
    </script>
  </head>
  <body>

    <!-- <h1>정현진님</h1>
     <table>
       <tr>
         <th>국어점수</th>
         <th>영어점수</th>
         <th>수학점수</th>
         <th>평균</th>
         <th>학점</th>
       </tr>
       <tr>
         <td>90</td>
         <td>90</td>
         <td>90</td>
         <td>90</td>
         <td>A등급</td>
       </tr>
     </table> -->

  </body>
</html>
